/**
 * Header 고정 높이(px). Header의 실제 높이, IntersectionObserver rootMargin 계산,
 * 앵커 스크롤 오프셋(lenis.scrollTo offset), `<main>` 상단 여백에서 공통 참조한다.
 * 값을 바꿀 때 이 상수 하나만 수정하면 모든 계산이 함께 갱신된다.
 */
export const HEADER_HEIGHT = 72;
